#grma1075 94731
#a) Es werden 2 Objekte in der Klasse erstellt
#b) 2 2 64 64
#c) Kommentare im Code 
#Klasse MyClass wird hier definiert 
class MyClass:
    #Hier wird ein Klassenattribut definiert
    x = 5
    #Hier wird das erste Objekt erstellt (Objekt1), p1.x=5
p1 = MyClass()
#Hier wird gesagt, dass p2 auf dasselbe Objekt wie p1 zeigen soll (Objekt1) p2.x=5
p2 = p1
#Hier wird das zweite Objekt erstellt (Objekt2), p3.x=5
p3 = MyClass()
#p4 zeigt jetzt auf dasselbe Objekt wie p3 (Objekt2) p4.x=5
p4 = p3
#p1 zeigt jetzt auf dasselbe Objekt wie p4 (Objekt2) p1.x=5
p1 = p4
#Klassenattribut wird überschrieben vom Instanzattribut, da p1, p3 und p4 alle auf Objekt2 zeigen, sind p1.x, p3.x und p4.x alles 1
p1.x = 1
#Klassenattribut wird überschrieben vom Instanzattribut, p2.x = 2
p2.x = 2
#Die Referenz von p1 wird nochmal geändert, p1 zeigt jetzt aus dasselbe Objekt wie p2 (Objekt1)
p1 = p2
#DA p1.x=2 ist, wird hier der Wert von x von Objekt2 auf 8 gesetzt
p3.x = p1.x ** 3
#Da p4 auch auf Objekt2 zeigt, wird der Wert von x von Objekt2 auf 64
p4.x = p4.x ** 2
#p1 zeigt auf Objekt1 p1.x=2
#p2 zeigt auf Objekt1 p2.x=2
#p3 zeigt auf Objekt2 p3.x=64
#p4 zeigt auf Objekt2 p3.x=64
print(p1.x, p2.x, p3.x, p4.x)